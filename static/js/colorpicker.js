
// vim: set ts=4 sw=4 et:
"use strict";

function ColorPicker( p_session, p_picker_id ) {
	var self = this;

	this.update_size = function() {
        // container 
        
	}

    this.to_s = function() { JSON.stringify(this); };

	this.session = p_session;

    var container_id = picker_id
    var container = document.getElementById( p_picker_id );
}

