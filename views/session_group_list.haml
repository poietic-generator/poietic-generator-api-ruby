- # vim: set ts=2 sw=2 et:

%div(data-fullscreen="true" data-role="page" class="ui-page poietic-page-#{@page.title}")
  %div(data-role="header" data-id="head")
    %h1.username Choose session
    %a.ui-btn-left.logout(href="/" data-rel="back") Back
  %div(role="main" class="ui-content") 
    %ul(data-role="listview" class="ui-listview")
      - @group_list.each do |group|
        %li
          %a(href="/session/#{group.token}/draw" data-ajax="false") 
            %h2= group.canonical_name
            %p 
              - if group.live? and group.live_users_count > 0
                = "%d users live!" % group.live_users_count
              - else
                No user connected

      //%a#link_choose{"data-ajax" => "false", "data-theme" => "b", "data-inline" => "true", "data-role" => "button", :href => "/page/list"} 
      //%button(type="submit" data-theme="b" name="submit" value="submit-value")
      //  Join a session
 
