<!DOCTYPE html>
<html>
	<head>
		<%= 
			@page.css.insert 0, "/css/base.css" 
			@page.render_css 
		%>
		<script type="text/javascript">
			var session = null;
			window.onload = function() {
				session = new Session();
				drawzone = new DrawZone(session, 'generator-session-drawzone');
				globalzone = new GlobalZone(session, 'generator-session-global');
				colorzone = new ColorZone(session, 'generator-session-colors');
				// create session with 
			}
		</script>
	</head>
	<body>
		<div id="page">
			<div id="header">
				<h1>Poietic Generator <em>Reloaded</em></h1>
			</div>
			<div id="content">
